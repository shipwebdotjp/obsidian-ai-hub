# CONTEXT.md — タスクオーケストレーション

この文書は obsidian-ai-hub のドメイン用語集と境界の定義である。実装詳細は含まない。
仕様は [docs/task-agent/specification.md](docs/task-agent/specification.md)、判断の記録は [docs/task-agent/adr/](docs/task-agent/adr/) と
[ai_wiki/](ai_wiki/00-Index.md) を参照すること。文書群の入口は [docs/task-agent/README.md](docs/task-agent/README.md)。

## Bounded Context

- **タスクオーケストレーション (Task Orchestration)** — 本文書の主対象。ユーザーの自由文依頼を理解・分解し、
  計画の承認を経て、Vault と登録済み Git リポジトリへ委譲実行する文脈。
- **Vault 管理** — 日次ノート、Inbox、要約、検索インデックス。タスクからは Workspace として参照される。
- **HITL 基盤** — 質問セットと回答の汎用永続化 ([ai_wiki/10-Decisions-HITL.md](ai_wiki/10-Decisions-HITL.md))。
  タスクオーケストレーションからは利用者であり、HITL Request の格納を委ねる。
- **Coding Workspace** — 既存のコーディングオーケストレーターと CLI セッション管理。
  タスクオーケストレーションからは委譲先の一種として利用される。
- **人物・記憶・リサーチ** — 本文書の対象外。タスクからは Capability 経由で間接的に触れるのみ。

## ユビキタス言語

| 用語 | 定義 |
| --- | --- |
| **タスク (Task)** | ユーザー依頼 1 件を表す集約ルート。投入から終端状態までの状態・計画・記録を所有する。 |
| **Task Agent (タスクエージェント)** | タスクオーケストレーション機能全体の名称。オーケストレーター・常駐ワーカー・CLI 入口・WebUI HITL 統合を指す。 |
| **オーケストレーター (Orchestrator)** | タスクを理解・分解・中継する LLM ツール呼出しループと、アプリケーション側の永続状態機械。Task Agent の判断部分。 |
| **常駐ワーカー (Resident Worker)** | AI Hub 内でタスクを非同期に取り出して実行する常駐プロセス。オーケストレーターを駆動するが、判断はしない。 |
| **計画 (Plan)** | 目的・手順・使用ツールまたは委譲先・対象・想定変更と副作用・完了条件を含む実行計画。承認の単位。 |
| **ステップ (Step)** | 計画内の個別手順。MVP では計画の一括承認が前提で、ステップ単位の承認は行わない。 |
| **委譲 (Delegation)** | オーケストレーターが Capability を通じて委譲先へ作業を任せること。記録は親タスクのトレースに残る。 |
| **委譲先 (Delegate)** | 委譲を受ける側。登録済み専門エージェントまたはコーディング CLI。 |
| **Capability** | 委譲可能な能力の登録記述。Vault 操作・専門エージェント・コーディング CLI を同列に扱うための抽象。 |
| **Capability Manifest** | Capability の登録内容。少なくとも名前・説明・入力仕様・必要権限・アダプター種別・リトライ方針を表現する。 |
| **Adapter (アダプター)** | 委譲先ごとの入出力変換層。委譲先の結果を親タスクの状態・成果物・トレースへ正規化する。 |
| **Workspace (ワークスペース)** | タスクが作用する対象領域。Obsidian Vault または `project_path` を持つ Git リポジトリ。 |
| **プロジェクト (Project)** | 既存の確定済みプロジェクト記録（`projects` テーブル）。`project_path` を持つものだけが Workspace になり得る。 |
| **HITL Request** | エージェントが人間に出す質問・計画承認・差戻し・取消要求の単位。WebUI の受信箱で処理される。 |
| **差戻し (Rejection / Rework)** | 承認前に計画を却下し、理由を必須として同じタスク ID のまま再計画させること。 |
| **再承認 (Re-approval)** | 承認済み計画からの逸脱が必要になった際、改訂計画を改めて承認してもらうこと。 |
| **協調的取消 (Cooperative Cancellation)** | 実行中タスクに対し、新しいステップを開始せず実行中ツールへ中止要求し、途中変更を記録する取消方式。 |
| **中断 (Interrupted)** | 常駐ワーカー停止時に実行中だったタスクの状態。再起動後に自動再実行されない。 |
| **Trace Event** | タスクの監査記録となる構造化イベント。SQLite 上の正本。機密値は redact される。 |
| **成果物 (Artifact)** | タスクが生んだ要約・変更一覧・ファイル等の結果物。 |

## 集約・エンティティ・値オブジェクト

- **Task（集約ルート）** — Plan・Step・HITL Request・Delegation 記録・Trace Event を従属させる。
  タスクの状態は Task 上でただ一つ。
- **Plan / Step（エンティティ）** — Task に従属。改訂は同一 Task 内の新しい版として扱う（タスク ID は不変）。
- **Capability（エンティティ）** — Task から独立した参照データ。登録・更新はタスク実行と別のライフサイクルを持つ。
- **Workspace（値オブジェクト）** — 解決済みの対象パスと種別（vault / git repo）。ロックの鍵になる。
- **HITL Request（エンティティ）** — Task に従属しつつ、HITL 基盤の Run と対応する。

## 不変条件

- Task は常にちょうど 1 つの状態を持つ。終端状態（完了・失敗・取消）から遷移しない。
- Plan は人間の承認なしに実行へ進まない。差戻しには理由が必須。
- 同一 Workspace への書込みは同時に 1 つのみ。読取は並列可。
- ツールの自動リトライは Capability Manifest／Adapter が宣言した範囲内でのみ行われる。
- Trace Event は追記のみで改変しない。秘密値を含まない。
- タスク ID は差戻し・再計画・再承認を通じて不変。

## 外部境界

- **CLI 入口** — 自由文プロンプトの投入専用。投入後の対話はしない。
- **WebUI HITL** — 質問回答・計画承認・差戻し・取消の唯一の人間インターフェース。ポーリング前提。
- **Obsidian Vault** — `config.yml` に登録された Vault。読取・作成・編集・削除が可能。
- **Git リポジトリ** — 有効な `project_path` を持つ確定済みプロジェクトのみ。
- **コーディング CLI** — Codex / OpenCode 等。実権限は CLI 固有設定を信頼する（親は保証しない）。
- **LLM プロバイダ** — 既存 AI Hub の秘密管理を使用。

## 非推奨な言い換え

- Task を「ジョブ」「セッション」と呼ばない。セッションは coding / agent の会話単位の既存用語。
- 「承認」をステップ単位に使わない。MVP の承認は計画全体の一括承認。
- 取消を「ロールバック」と同義にしない。取消はロールバックを意味しない。
- 常駐ワーカーを「オーケストレーター」と混同しない。ワーカーは駆動装置、オーケストレーターは判断。
- 委譲先を「ツール」と混同しない。ツールはオーケストレーターの LLM ループ内の呼出し、委譲は Adapter を経由する作業移譲。
- HITL Request を「通知」と呼ばない。MVP に外部通知は存在しない（WebUI ポーリングのみ）。
