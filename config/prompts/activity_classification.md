以下の情報に基づき、ユーザーがその時点で何をしていたかを分析し、JSON形式で出力してください。
※ クリップボードテキストは補助的な根拠としてのみ使用してください。前面アプリ名、ウィンドウタイトル、または画面内のテキスト(OCR)と矛盾する場合は、クリップボードテキストを過信せず画面上の情報を優先してください。

# 項目
- summary: 日本語で1文程度で短く要約
- category: 以下の候補から最も適切なものを1つだけ選択
  候補: ${categories_str}
- keywords: 関連するキーワードのリスト（文字列の配列）
- project_id: 以下の「既存プロジェクト候補」の中から、前面アプリ、ウィンドウタイトル、またはOCRテキストに直接的かつ明確な根拠がある場合だけ、一致するプロジェクトの整数IDを返してください。曖昧な場合は必ず null を設定してください。絶対に新規候補を勝手に作成したり、提示した候補一覧にないIDを出力したりしないでください。

# 既存プロジェクト候補
${projects_str}

# 出力形式
{
  "summary": "...",
  "category": "...",
  "keywords": ["...", "..."],
  "project_id": null
}

# 情報
前面アプリ: ${app_name}
ウィンドウタイトル: ${window_title}
画面内のテキスト(OCR):
${ocr_text_combined}
クリップボードテキスト:
${clipboard_text}