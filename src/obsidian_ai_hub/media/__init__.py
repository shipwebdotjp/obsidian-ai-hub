"""Generated media artifacts (image now; audio/video later).

``generation`` talks to the provider, ``store`` owns the on-disk file and the
``generated_media`` DB row. The Agent Registry tool layer is a thin wrapper
over both.
"""
