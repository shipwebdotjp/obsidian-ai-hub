  順位    施策                                                 区分                優先理由
  ━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
      1    実行の安全性・停止契約をv1仕様どおりに完成           v1未充足            Capability 実行への取消伝播、activation_id による冪等性、backoff_seconds、attentionの「採用」時の再検証を整える。副作用の重複や
                                                                                    「取消したのに動き続ける」を防ぐ最優先。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      2    Scheduler Job から公開Workflowを起動                 計画済み            手動実行専用から、定期リサーチ・日次整理など本来の自動化へ広がる。承認が必要なWorkflowは毎回 waiting_approval に止める方針を明確
                                                                                    にする。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      3    JSON入力中心のエディタをガイド型にする               未計画・UX          Capability/Agent の入力、出力Schema、Loop設定、型付き参照が生JSON編集中心で、作成の最大の障壁。Schemaフォーム、参照ピッカー、入
                                                                                    力候補、設定テンプレートを優先する。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      4    承認待ち・HITL待ち・失敗・完了の通知outbox           計画済み            Schedulerと組み合わせて初めて「放置しても運用できる」状態になる。LINE/PushにはRunへの深いリンクを付ける。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      5    Run履歴の一覧・絞り込み・Activity連携                v1未充足＋未計画    現状はWorkflow詳細で直近10件のみ。状態・期間・Revision・再実行元で追えないと、継続利用時の監査と復旧が難しい。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      6    Workflow本体の名前・説明の更新と削除                 v1未充足            仕様上はWorkflow CRUDだが、現在は作成・一覧・取得のみ。Revision削除はあるが、不要なWorkflowの整理や改名ができない。削除はRunス
                                                                                    ナップショットを残す契約にする。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      7    ユーザー管理テンプレート＋JSON/YAML export/import    計画済み            現在はコード定義の2テンプレートのみ。検証済み定義を再利用・バックアップ・共有できるようにすると作成効率が上がる。importは必ず
                                                                                    draftとして取り込んで再検証する。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      8    Agent Nodeの構造化出力を堅牢化                       未計画              現状はJSON不適合で即失敗する。JSON Schemaをより確実に守る出力モード、限定的な修復再試行、失敗内容の分かりやすい表示が実用性を上
                                                                                    げる。
  ──────  ───────────────────────────────────────────────────  ──────────────────  ──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────────
      9    文書と実装状態の同期                                 即時の小修正        docs/workflow の「未実装」表記、およびガイドの「conditionはUI編集不可」は実態と不一致。利用者の判断を誤らせるため、次の変更と切
                                                                                    り離して更新してよい。

  後回しでよいものは、Loopネスト、Agentごとのprompt/model/tool上書き、並列fork/AND join、複雑なJSON Schema、任意コードNodeです。どれも安全境界・検証・UIの複雑さを大きく増やす割に、現時点の基本利用価値を直接は高め
  ません。