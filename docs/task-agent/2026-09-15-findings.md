[ocr] Summary: 74 file(s) reviewed, 28 comment(s), ~9445722 token(s) used (input: ~9256852, output: ~188870), cache(read: ~8103655, write: ~0), 7m23s elapsed
[ocr] Session: fc06b482-5ad6-40c0-bfcb-4f740e2471f8
Review complete: 28 finding(s) across 74 selected item(s).

─── tests/test_tasks_orchestrator.py:800-801 ───
[test · medium] Duplicate test name overwrites earlier definition:
`test_resume_with_gapped_indices_continues_after_max` is defined twice (lines ~572 with indices
(0,3)->4 and ~800 with indices (0,2)->3). In Python the second def replaces the first, so the first
case is never collected — silent coverage loss with misleading green pass. Rename one (e.g.
`..._after_max_0_3` / `..._gapped_0_2`).

- def test_resume_with_gapped_indices_continues_after_max():
+ def test_resume_with_gapped_indices_continues_after_max_0_2():
      task = store.create_task("再開ジョブ")


─── tests/test_tasks_orchestrator.py:877-880 ───
[test · medium] Duplicate test name overwrites earlier definition:
`test_deviation_rebuild_preserves_completed_targets` is defined twice (~633 delegate job asserting
preserved inputs=={"task":"first"} vs ~877逸脱ジョブ with different revised plan). Only the last runs,
discarding the first deviation-rebuild contract. Rename to keep both cases collected.

- def test_deviation_rebuild_preserves_completed_targets():
+ def test_deviation_rebuild_preserves_completed_targets_vault_search():
      from obsidian_ai_hub.tasks.execution import DeviationReported

      task = store.create_task("逸脱ジョブ")


─── frontend/src/features/task-agent/CapabilitySettingsPage.tsx:179-180 ───
[security · high] Privileged capabilities (vault_write_file / memory_propose / specialist_agent /
coding_cli) can be switched to `auto` in this UI with no frontend guard. Backend `update_capability`
in store.py only validates `auto|plan_required` and `sync_capabilities` never overwrites DB policy,
so a downgrade persists and bypasses plan approval for writes/delegation/execution. Add a frontend
guard (disable `auto` or confirm) for plan_required-default keys from capabilities.py, and enforce
server-side that privileged keys cannot be downgraded.

-                       <option value="auto">auto(承認不要)</option>
-                       <option value="plan_required">plan_required(Plan一括承認)</option>
+ const PLAN_REQUIRED_ONLY = new Set(["vault_write_file", "memory_propose", "specialist_agent", "coding_cli", "research_agent"]);
+ // in row render:
+ // <option value="auto" disabled={PLAN_REQUIRED_ONLY.has(c.capability_key)}>auto(承認不要)</option>


─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:488-491 ───
[bug · medium] HITL onSubmit/onCancel swallow failures: try/finally resets `hitlBusy` without catch
or error state, and `waitForHitlSettled(...).catch(()=>undefined)` never throws. A failed
`submitHitlAnswer`/`cancelHitlRun` therefore clears the busy state silently with stale questions and
no feedback. Other callers (useAgentSessions, useCodingSessionDetail, HitlPage) try/catch and
surface via onChatError/onError/detailError, and WaitingRunQuestionCard expects rejected onSubmit to
show its internal error. Re-throw or set local error state instead of swallowing.

-                 await waitForHitlSettled(hitlRun.run_id).catch(() => undefined);
+ try {
+   await Promise.all(Object.entries(answers).map(([key, a]) => submitHitlAnswer(hitlRun.run_id, key, a.value, a.comment)));
+   await waitForHitlSettled(hitlRun.run_id);
                  await loadDetail(false);
+ } catch (e) {
+   setActionError(e instanceof ApiError ? e.message : "回答の送信に失敗しました");
+   throw e;
                } finally {
                  setHitlBusy(false);
+ }


─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:145-145 ───
[bug · medium] `payload: Record<string, any>` weakens type safety (checklist bans `any`) and
downstream `e.payload?.action_index` / `childRunRefFromPayload(e.payload)` accesses are unchecked.
Backend/frontend `TaskAgentEvent.payload` is already `Record<string, any>`, so this propagates `any`
through the detail view and risks runtime errors on shape drift. Use `Record<string, unknown>` with
narrow helpers (as done by `asText`) instead.

- function RelatedRunLinks({ payload }: { payload: Record<string, any> }) {
+ function RelatedRunLinks({ payload }: { payload: Record<string, unknown> }) {


─── frontend/src/features/task-agent/TaskAgentListPage.tsx:61-66 ───
[performance · medium] Non-terminal filter is client-side after `limit: 100` fetch, and `total` is
set to `visible.length`. Tasks beyond the first page are hidden and counts are misreported as volume
grows. `listTaskAgentTasks`/`store.list_tasks` already support server-side `status`/`limit`/`offset`
+ `total`. Either add server support for non-terminal query or paginate/fetch with offset until the
filter is satisfied.

-       let visible = res.items;
-       if (filter === NON_TERMINAL_FILTER) {
-         visible = res.items.filter((t) => !TERMINAL_SET.has(t.status));
-       }
-       setItems(visible);
-       setTotal(filter === NON_TERMINAL_FILTER ? visible.length : res.total);
+ // Prefer server-side filtering/pagination, e.g. loop with offset until limit satisfied
+ // or add `status=__non_terminal__` support to the list API and use res.total directly.


─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:238-239 ───
[performance · medium] 3s `setInterval(() => loadDetail(false))` has no overlap/abort guard, and
each tick also fires nested `getHitlRun`. Slow responses can overlap, race on row switches, and
overwrite newer detail/HITL state with stale data. Other pages guard with op-session refs / settled
checks. Add an in-flight flag or AbortController and skip ticks while a load is pending or taskId
changed.

-     const id = setInterval(() => void loadDetail(false), 3000);
+ const inflight = useRef(false);
+ const id = setInterval(() => {
+   if (inflight.current) return;
+   inflight.current = true;
+   void loadDetail(false).finally(() => { inflight.current = false; });
+ }, 3000);
      return () => clearInterval(id);


─── frontend/src/features/task-agent/ChildRunLink.tsx:69-73 ───
[maintainability · low] Agent vs coding branches are near-duplicates (only `agentSessionPath` vs
`codingSessionPath` differ) plus repeated meta-text building. When adding new `child_kind` mappings
this drifts. Collapse to a single parameterized helper that resolves path by kind via ROUTES.

-   if (childKind === "agent" && sessionId) {
-     return (
-       <span className="min-w-0 break-words [overflow-wrap:anywhere]">
-         <Link
-           to={agentSessionPath(sessionId)}
+ function childSessionPath(childKind: string, sessionId: string): string | null {
+   if (childKind === "agent") return agentSessionPath(sessionId);
+   if (childKind === "coding") return codingSessionPath(sessionId);
+   return null;
+ }


─── frontend/src/features/task-agent/ChildRunLink.tsx:61-61 ───
[style · low] Loose `!= null` violates strict-equality rule (`===`/`!==` only). It also masks
`undefined` vs `null` differences for optional run/session fields. Use `!== null && !== undefined`
(or `!= null` cleanup across the feature).

-   if (projectId != null && String(projectId) !== "") {
+   if (projectId !== null && projectId !== undefined && String(projectId) !== "") {


─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:118-118 ───
[style · low] Same loose-equality issue as ChildRunLink: `!= null` is used for `max_actions`,
`active_child_run_id`, `hitlRun`, and `stepLabel`. Replace with strict checks to comply with
`===`/`!==` rule.

-       {plan.max_actions != null && (
+       {plan.max_actions !== null && plan.max_actions !== undefined && (


─── frontend/src/features/task-agent/CapabilitySettingsPage.tsx:13-13 ───
[maintainability · low] `CAPABILITY_DESCRIPTIONS` hardcodes Japanese strings keyed by capability_key
that can drift from the canonical catalog in `capabilities.py`/`capability_schemas.py` (e.g.
registry-derived keys, renamed specials). Consider deriving labels from the list-capabilities API or
sharing a single source, falling back to `adapter_kind` only for unknown keys.

- const CAPABILITY_DESCRIPTIONS: Record<string, string> = {
+ // TODO: prefer label/description from listTaskAgentCapabilities() or backend catalog;
+ // keep this map only as UI fallback.


─── frontend/src/features/task-agent/StructuredValue.tsx:33-40 ───
[performance · low] `SmartText` calls `JSON.parse` via `tryParseJsonObjectOrArray` on every render
for prompt/result/plan text. On large histories this repeats parse cost per row. Memoize the parse
(useMemo) or hoist parsing out of the hot list render.

- export function SmartText({
-   text,
-   className,
- }: {
-   text: string;
-   className?: string;
- }) {
-   const parsed = tryParseJsonObjectOrArray(text);
+ import { useMemo } from "react";
+ const parsed = useMemo(() => tryParseJsonObjectOrArray(text), [text]);


─── frontend/src/features/task-agent/TaskAgentCreateForm.tsx:34-34 ───
[bug · low] Validated with `trim()` but submits raw `promptText`, so leading/trailing
whitespace/newlines are sent to the server. Trim before POST to avoid storing padded prompts and to
match the `canSubmit` guard.

-       const task = await createTaskAgentTask({ prompt_text: promptText });
+       const task = await createTaskAgentTask({ prompt_text: promptText.trim() });


─── src/obsidian_ai_hub/coding/service.py:21-23 ───
[maintainability · low] Unused import `parse_cli_request` — service now parses via
`parse_coordinator_response` only (worker already dropped the legacy import). Remove it to keep the
canonical parser unambiguous.

      parse_and_normalize_worker_output,
      parse_coordinator_response,
-     parse_cli_request,


─── src/obsidian_ai_hub/web/services/task_agent.py:37-37 ───
[bug · medium] Lost update: get_task status check then decide_plan is check-then-act. decide_plan
re-reads the task but its task UPDATE is unconditional (WHERE task_id=? only), unlike
transition_task_status which uses WHERE task_id=? AND status=? + rowcount check. Concurrent approve
vs cancel (both read waiting_approval) can overwrite: cancel sets cancelled, then approve blindly
sets ready (or vice versa). Consider adding the same compare-and-swap guard to the decide path or
routing approve/reject through a status-guarded transition.

  def approve_task_agent_task(task_id: str) -> dict[str, Any]:
+     task = task_store.get_task(task_id)
+     if task is None:
+         raise FileNotFoundError(f"Task '{task_id}' not found.")
+     # Delegate the waiting_approval guard atomically to the store
+     # (store should UPDATE ... WHERE status IN (...) and raise on rowcount==0).
+     return task_store.decide_plan(task_id, "approve")


─── Makefile:110-110 ───
[maintainability · low] New serve-debug target is not listed in .PHONY (line 9 still ends with
serve). If a file named serve-debug ever exists, make will skip the recipe. Add serve-debug to
.PHONY.

- serve-debug:
+ .PHONY: install install-all install-hitl-worker start stop restart restart-hitl-worker reload reload-hitl-worker enable enable-hitl-worker disable disable-hitl-worker status status-hitl-worker logs logs-hitl-worker errorlogs errorlogs-hitl-worker build-web dev-web jules-setup serve serve-debug


─── src/obsidian_ai_hub/tasks/adapters/coding.py:302-310 ───
[bug · high] Session-scoped scan can return prior-run output: `run_id` is only used in the error
message, never to filter messages. With session reuse (`_resolve_session` returns `prior, True`), a
current run that produced no `orchestrator` messages falls through to the newest message from a
previous run, producing a stale summary and potentially hiding a missing deviation report.
`coding_messages` already carries `run_id` (SELECT * includes it), and the agent adapter correctly
scopes via `assistant_message_id` — filter here the same way.

      def _final_text(self, coding_store: Any, session_id: str, run_id: str) -> str:
          messages = coding_store.list_messages(session_id)
          for message in reversed(messages):
+             if str(message.get("run_id") or "") != run_id:
+                 continue
              if str(message.get("role") or "") != "orchestrator":
                  continue
              text = str(message.get("content") or "")
              if text.strip():
                  return text
          raise ValueError(f"Child coding run '{run_id}' produced no messages.")


─── src/obsidian_ai_hub/tasks/adapters/deviation.py:62-72 ───
[security · high] Untrusted child steps are persisted without schema validation: only
`capability_key` membership and `target`/`inputs` being dicts are checked.
`validate_capability_target`/`validate_capability_inputs` (single source in `capability_schemas.py`)
are not called, and there is no count/size bound. `execution.py`/`orchestrator.py`
`create_plan(dev.revised_plan)` stores this verbatim for `waiting_reapproval`, so malformed,
oversized, or scope-widening (e.g. different `project_id`/`agent_id`) steps persist until human
review/execution instead of being refused here as the docstring intends.

      allowed = get_capability_keys()
+     from obsidian_ai_hub.tasks.capability_schemas import (
+         validate_capability_inputs,
+         validate_capability_target,
+     )
+     if len(report["steps"]) > 20:
+         raise ValueError("deviation_request steps list too large.")
      for index, step in enumerate(report["steps"]):
          if not isinstance(step, dict):
              raise ValueError(f"Reported step {index} must be an object.")
          key = step.get("capability_key")
          if key not in allowed:
              raise ValueError(f"Reported step {index} uses unknown capability '{key}'.")
-         if not isinstance(step.get("target"), dict) or not isinstance(
-             step.get("inputs"), dict
-         ):
-             raise ValueError(f"Reported step {index} target/inputs must be objects.")
+         try:
+             step["target"] = validate_capability_target(str(key), step.get("target"))
+             step["inputs"] = validate_capability_inputs(str(key), step.get("inputs"))
+         except ValueError as exc:
+             raise ValueError(f"Reported step {index} {exc}") from exc


─── tests/test_coding_coordinator_contract.py:11-14 ───
[style · low] Unused imports `asyncio` and `AsyncMock` add maintenance noise; neither is referenced
outside the import lines.

- import asyncio
  import json
  import subprocess
- from unittest.mock import AsyncMock, MagicMock, patch
+ from unittest.mock import MagicMock, patch


─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:213-218 ───
[bug · medium] Silent `catch { setHitlRun(null) }` hides HITL load failures: user sees no question
card and no error, indistinguishable from "no questions". Surface a load error (e.g. reuse
`error`/`actionError` state) or retry on next poll.



─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:202-203 ───
[bug · medium] `rejectReason`/`actionError` are never reset when `taskId` changes (same mounted
panel in master-detail). Previous task's reject reason/error banner carries over to the next task.
Clear them in `loadDetail` or a `useEffect([taskId])`.



─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:285-289 ───
[maintainability · medium] `childRefEvents` re-implements `childRunRefFromPayload` filtering but
string-only, while the helper accepts numbers via `asText`. Numeric `child_run_id` would show in
Event list via `RelatedRunLinks` but be missing here. Reuse `childRunRefFromPayload(e.payload) !=
null` as the filter.



─── frontend/src/features/task-agent/TaskAgentDetailPanel.tsx:81-82 ───
[maintainability · low] Using array index `key={i}` for capabilities is unstable on reorder/insert;
`capability_key` is available and stable. Use it (with index fallback only when missing).



─── frontend/src/features/task-agent/StructuredValue.tsx:95-100 ───
[performance · medium] No depth/item cap: every event/plan payload renders fully via recursion. A
large `capability_completed` payload or long history can create a huge DOM (detail renders
StructuredValue per event + per action + per plan + snapshots). Add depth limit and array truncation
(e.g. first N + "... N more").



─── src/obsidian_ai_hub/tasks/orchestrator.py:249-252 ───
[maintainability · low] Unused `scope` parameter: the body renders allowlists and intents from
`plan` directly and never reads `scope` (caller already derives it via `approval_scope(plan)`).
Remove the parameter or use it to avoid a misleading signature.

  def build_orchestrator_prompt(
      task: dict[str, Any],
      plan: DirectionalPlan,
-     scope: dict[str, Any],


─── src/obsidian_ai_hub/tasks/orchestrator.py:21-21 ───
[maintainability · low] Unused import `Field`: `RuntimeAction` uses only plain annotations (`reason:
str = ""`) and the file never references `Field` (remaining matches are Japanese prose containing
“field”). Remove it from the import to avoid dead code.

- from pydantic import BaseModel, ConfigDict, Field
+ from pydantic import BaseModel, ConfigDict


─── src/obsidian_ai_hub/tasks/planning.py:662-665 ───
[bug · medium] `_fail_task` unconditionally transitions to `failed`, but `planning` can be cancelled
to `cancelled` while the planner LLM call is in flight (cancel API maps non-running tasks directly
to `cancelled`, and `cancelled` has no outgoing transitions). In that race
`transition_task_status(..., "failed")` raises `ValueError: Illegal transition`, masking the
original planner error and turning the expected `ValueError: no longer in planning` path into an
unexpected failure. Guard with a status check (only mark failed when still in `planning`) or swallow
illegal-transition like the success path does.

          def _do() -> None:
+             current = task_store.get_task(task_id, conn=active_conn)
+             if current is None or str(current["status"]) != "planning":
+                 return
              task_store.transition_task_status(
                  task_id, "failed", error_summary=str(exc), conn=active_conn
              )


─── src/obsidian_ai_hub/tasks/hitl.py:30-30 ───
[bug · medium] Blind `waiting_user -> queued` requeue can raise after cancel: `waiting_user` is
cancellable to `cancelled` (terminal, no outgoing transitions), while the linked HITL run stays
answerable (planner comment notes orphaned question runs). If the user cancels then answers,
`transition_task_status(..., "queued")` raises `Illegal transition: cancelled -> queued` and fails
the HITL resume. Check current status first (no-op when already `queued`, skip when
terminal/cancelled) before transitioning.

+     current = task_store.get_task(str(task_id), conn=ctx.conn)
+     if current is None:
+         return HitlResult.complete()
+     if str(current["status"]) != "waiting_user":
+         return HitlResult.complete()
      task_store.transition_task_status(str(task_id), "queued", conn=ctx.conn)